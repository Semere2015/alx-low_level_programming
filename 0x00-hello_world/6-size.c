#include <stdio.h>

/**
 * main - size of data types
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
sizeof(char);
sizeof(int);
sizeof(long int);
sizeof(long long int);
sizeof(float);
return (0);
